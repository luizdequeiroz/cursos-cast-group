namespace domain.Enums
{
    public enum Code
    {
        SUCCESS = 0,
        ITEM_DOES_NOT_EXIST = -1,
        ITEM_NOT_DELETED = -2,
        UNLISTED_ITEMS = -3,
        ITENS_NOT_FOUND = -4,
        INVALID_INPUTS = -5,
        BUSY_PERIOD = -6,
        INVALID_DATE_RANGE = -7,
        INVALID_START_DATE = -8
    }
}
